# Copied from rc.d/aliases to overwrite zsh defaults

if command -v eza > /dev/null; then
  alias ls='eza'
  alias ll='eza -alh --git'
  alias tree='eza --tree'
else
  alias ll='ls -alF'
  alias la='ls -A'
  alias l='ls -CF'
fi
